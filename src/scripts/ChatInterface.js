class ChatInterface {
  contructor() {
    //Set up socket/api connection here and other variables needed
    //  such as the target (defined in extenders) for the Chat MutationObserver
  }

  interfaceMethods() {}

  extendorsMethodSkeletons() {}
}
